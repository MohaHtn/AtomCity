package org.arcade.atomcity.domain.usecase

import org.arcade.atomcity.data.MaiteaRepository

class GetMaiteaDataUseCase(private val repository: MaiteaRepository) {
    // Add methods as needed by the app
}
